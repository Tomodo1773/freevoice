# CLAUDE.md

## 言語設定

常に日本語で回答してください。

## 実装方針

- 一般公開するOSSツール。不特定多数のユーザーが利用する前提で品質を担保する
- シンプルさを優先しつつ、堅牢性とのバランスを取る
- YAGNI、KISS、DRYを徹底せよ。
- 状態管理は明示的に行い、暗黙の遷移や競合が起きない設計にする
- 起こりうる状況すべてに個別挙動を与えるのは品質ではない。問うのは「何が正しい挙動か」ではなく「この状況に個別対応する価値があるか」。実際のユーザーがほぼ遭遇しない、または一律の単純挙動（無視・一律エラー・そのまま進める）で困らない状況は個別対応しない。個別挙動は1つごとに状態とガードを増やす
- 上記を通過した本当に必要なケースは、ガードや約束事で守るのではなく、問題の状況が構造的に発生しえない設計で対処する（例: ADR 0001 の単一ジョブ所有）
- 同じ箇所への修正が繰り返されるのは根本原因が設計にある兆候。付け焼刃で状態やガードを足す前に、まず上の2つの問いに立ち返る
- エラーハンドリングとセキュリティは適切に実装する
- サイクロマチック複雑度（循環的複雑度）を増大させずに済む場合を除き、後方互換性のためのシムやフォールバックパスは設けないこと。

## テスト方針

- 正常系を中心にテストを実装
- ユーザー操作の競合やタイミング依存の処理は異常系もテストする
- 外部リソース中心でモック中心になるテストは見送る

## アプリ概要

FreeVoice は Windows 専用の音声入力ツール。`Ctrl+Shift+Space` を押している間だけ録音し（Push-to-Talk）、離すと AI で後処理して現在フォーカス中のテキストフィールドに貼り付ける。

## コマンド

```bash
# 開発サーバー起動（フロントエンド + Tauri）
pnpm tauri dev

# プロダクションビルド
pnpm tauri build

# フロントエンドのみビルド
pnpm build

# 型チェック
pnpm tsc --noEmit
```

## アーキテクチャ

### 2ウィンドウ構成

| ウィンドウ | label | エントリポイント | 役割 |
|-----------|-------|----------------|------|
| 設定画面 | `main` | `index.html` → `src/main.tsx` → `App.tsx` | 初期非表示。トレイから開く |
| オーバーレイ | `overlay` | `overlay.html` → `src/overlay-main.tsx` → `Overlay.tsx` | 初期非表示。録音中のみ表示。クリック透過・常前面 |

### フロントエンド / Rust の責務分担

ロジックは可能な限り全てフロントエンドに、Rust で書く内容は最小限にする。
